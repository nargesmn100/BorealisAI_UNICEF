# UNICEF × RBC Borealis AI — Jamaica Child Deprivation Pipeline
# Python >= 3.10 required (uses union type hints: str | None)

# Core data manipulation
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Geospatial
geopandas>=0.14.0
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.6.0
pyogrio>=0.6.0       # fast GeoPackage / GPKG reader

# ML
scikit-learn>=1.3.0

# GBM (optional but recommended — at least one is needed for GBM model)
lightgbm>=4.0.0
# xgboost>=2.0.0    # uncomment as alternative to lightgbm

# GAM (optional — needed for gam_model.py)
pygam>=0.9.0

# Interpretability (optional)
# shap>=0.43.0      # for SHAP analysis in GBM model

# Config
pyyaml>=6.0

# Data loading (Excel files)
openpyxl>=3.1.0

# Output serialisation
pyarrow>=12.0.0      # for Parquet I/O

# Visualisation (needed for notebooks/01_analysis.ipynb)
matplotlib>=3.7.0
folium>=0.14.0         # for interactive HTML map output
branca>=0.6.0          # colour scales for folium maps
